---
layout: base
title: indice
---
#terminal.tiny
  #title
    >_
  #content
    %ul
      %li
        %span 
          >_
          %a(href="/")
            ls
      - site.posts.each do |post|  
        %li
          %span
            >_
            %a{:href => post.url }
              = post.title
          - categories(post)
          %span.date= post.date.strftime("%d/%m/%Y")
#sidebar
  .block  
    - site.categories.each do |category|
      %a.category{:href => "/#{category.first}.html"}
        %img{:src => "/images/script-icons/#{category.first}.png", :width => "50px", :title => "ir a categoria #{category.first}" }
    .clear
  #twitter
  .clear